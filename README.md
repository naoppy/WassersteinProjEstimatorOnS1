# Wasserstein projection estimators for circular distributions

Implementation of [Wasserstein projection estimators for circular distributions](https://arxiv.org/abs/2510.18367).

## Installation
download `uv`, run `uv sync`.

## Computational Environment
This code uses only CPU. No GPU is required.
Tested on Linux and Windows systems.

Computation time may be long depending on the parameters.
We used strong workstation with many cores to run the experiments.

## Run Scripts
`python -m src.experiments.ex6_vonmises_MSE -O`

You should not specify .py suffix.

## LICENSE
Author: Naoki Otani

This repository is provided by MIT License.

